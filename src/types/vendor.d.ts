// Ambient module declarations for document-parsing libraries that ship without
// TypeScript types (or whose internal entrypoint we import to avoid side effects).
declare module "mammoth";
declare module "pdf-parse/lib/pdf-parse.js";
